See `CLAUDE.md` in this same directory — it is the maintained, detailed
project-context doc (architecture, database, WhatsApp boundary, Redis phase
status, dashboard API contract, conventions). This file exists only so
agents/tools that specifically look for `AGENTS.md` find it; keep the real
content in `CLAUDE.md` and do not fork a second copy here.
